---
description: >-
  Loads senior fullstack .NET standards for C# work in gymbro: Clean Architecture,
  CQRS, MediatR, domain richness, Result-based APIs. Complements the Agent Skill
  senior-dotnet-angular-fullstack.
globs:
  - "**/*.cs"
alwaysApply: false
---

# Fullstack standards (backend workspace)

When editing C# in this repository, follow the Agent Skill **senior-dotnet-angular-fullstack** at `.cursor/skills/senior-dotnet-angular-fullstack/SKILL.md` (Clean Architecture, module boundaries, CQRS/MediatR, FluentValidation, thin controllers, EF Core boundaries, Result pattern).

For Angular UI work, open the **GymBroPortal** workspace and use its `.cursor/rules/angular-frontend.mdc` and **figma-design-system.mdc** together with the same skill.

## Shared documentation (read before any task)

System facts live in the root `docs/` SSOT (routed by [`../../../docs/README.md`](../../../docs/README.md));
backend solution layout / build / patterns in [`../../docs/BACKEND.md`](../../docs/BACKEND.md).

| What you need | Read |
|---|---|
| Big picture, stack, risks | `../../../docs/SYSTEM_OVERVIEW.md` |
| Module boundaries, endpoints, dependencies | `../../../docs/MODULES.md` |
| Domain entities, EF filters, tenancy | `../../../docs/DATABASE.md` |
| Authorization, roles, permission matrix | `../../../docs/PERMISSIONS.md` |
| Lifecycle rules (plan/assignment/session/membership) | `../../../docs/BUSINESS_RULES.md` |
| Flows (onboarding, invites, sessions) | `../../../docs/USER_FLOWS.md` |
| Solution layout, build/run/migrate | `../../docs/BACKEND.md` |

After every change, update the **one** owning doc listed in `../../../docs/README.md`.
